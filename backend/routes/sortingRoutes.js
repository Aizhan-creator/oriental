const express = require('express');
const router = express.Router();

// Маршрут для обработки запросов сортировки
router.get('/', (req, res) => {
    // Обработка запросов сортировки
});

// Другие маршруты для сортировки, если это необходимо

module.exports = router;
